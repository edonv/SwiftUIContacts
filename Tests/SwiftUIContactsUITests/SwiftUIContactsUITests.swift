import XCTest
@testable import SwiftUIContactsUI

final class SwiftUIContactsUITests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIContactsUI().text, "Hello, World!")
    }
}
